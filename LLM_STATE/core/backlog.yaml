tasks:
- id: diagnose-claude-tui-invisible-after-work-phase-banner-on-a-fresh-machine
  title: 'Verify fix: re-test work-phase TUI hang on fresh machine after removing --add-dir and fixing path normalisation'
  category: infra
  status: blocked
  blocked_reason: No fresh-machine access right now; revisit when colleague is available to re-test
  dependencies: []
  description: |
    ## Status

    Both leading-hypothesis fixes have shipped:

    1. **Double-slash path-join bug** (`select_config_dir` now normalises trailing slashes via `components().collect()`).
    2. **Redundant `--add-dir <plan_dir>`** removed from `invoke_interactive` in `src/agent/claude_code.rs`.

    These were the two most likely causes of the invisible trust-grant modal (hypothesis A) that hung the work phase on a fresh machine.

    ## Required action

    Have the affected colleague (macOS, local terminal, no multiplexer, Claude 2.1.119) pull the latest binary and re-run `ravel-lite run` from their launcher-web project.

    Expected outcome: work phase proceeds normally with no hang after the banner.

    ## If the hang persists

    Pivot to hypothesis B (inline-viewport handoff bug in `src/ui.rs:298` reset volley). Investigation path:

    - Check colleague's `~/.claude.json` for the project path to see if trust is already granted.
    - Run `dangerous: true` in their plan's config (appends `--dangerously-skip-permissions`); if hang disappears, a remaining trust-grant modal is the cause.
    - If B, inspect `src/ui.rs:244` Viewport::Inline and the `suspend` volley at line 298.

    The `capture-full-claude-stdio-under-a-pty-in-debug-log-for-tui-hang-diagnosis` task provides the full diagnostic capture tool if further investigation is needed.

    ## What "done" looks like

    Colleague confirms the hang is gone. Or, if hang persists: root cause identified and a follow-up fix landed.
- id: add-pty-capture-debug-path-to-pi-rs-invoke-interactive-parity-with-claude-code-rs
  title: Add PTY-capture debug path to pi.rs::invoke_interactive (parity with claude_code.rs)
  category: infra
  status: blocked
  blocked_reason: Not needed yet; defer until pi-side TUI debugging actually requires it
  dependencies: []
  description: |
    Add the same PTY-capture path to `pi.rs::invoke_interactive` that was
    built for `claude_code.rs` in the PTY-capture task. The mechanism is
    already implemented in `src/agent/pty_capture.rs`; this task is a
    small scope wiring job.

    ## Why

    When debug logging is enabled, `claude_code.rs::invoke_interactive` now
    routes through `pty_capture::run_pty_session`, capturing every byte
    exchanged with the claude subprocess in the debug log. The `pi.rs`
    equivalent still uses inherited stdio (`stdio: inherited (no transcript
    available)` per the Results from the PTY-capture task). A pi TUI hang
    would have the same diagnostic blind spot the claude hang had before the
    PTY work shipped.

    ## Scope

    - In `src/agent/pi.rs::invoke_interactive`, add an early branch:
      when `debug_log::is_enabled()`, call
      `pty_capture::run_pty_session("pi", &args, &cwd, "pi")` via
      `spawn_blocking`, matching the pattern in `claude_code.rs`.
    - Existing argv construction is shared; only the spawn mechanism differs.
    - Update the log entry text to read `stdio: pty (full byte transcript
      follows)` on the debug path, matching the claude path.

    ## Verification

    - `cargo test` passes (all PTY-capture unit/integration tests still
      pass; the pi tests are unaffected because none exercise the debug path).
    - Non-debug pi invocation: TUI renders identically, no behaviour change.
    - Debug-enabled pi invocation: TUI renders identically AND debug log
      captures the pi byte stream.
- id: replace-tutorial-illustrative-transcripts-with-testanyware-captured-transcripts
  title: Replace tutorial illustrative transcripts with TestAnyware-captured transcripts
  category: docs
  status: blocked
  blocked_reason: 'Capture script extended for per-command transcripts and validated against testanyware 0.2.0 CLI surface (the prior ''vm list --format json'' approximation was actually broken, now fixed via deterministic --id and --vm resolution). Remaining: a dedicated phase for the live VM run + per-chapter ---- block replacement in chapters 01-05.'
  dependencies: []
  description: |
    Replace the illustrative terminal transcripts in tutorial chapters
    `docs/tutorial/01-install-and-config.adoc` through
    `docs/tutorial/05-further-cycles.adoc` with real captures produced by
    `scripts/capture/ravel-lite-tutorial.sh` (which drives a TestAnyware
    macOS VM: installs via Homebrew, runs the tutorial scenario, pulls
    screenshots and the resulting LLM_STATE tree).

    The current chapters use realistic illustrative transcripts that match
    what the codebase produces and are publishable as-is. This task is the
    polish pass: replace each illustrative block with a verbatim terminal
    capture to give readers byte-accurate output.

    ## Prerequisite

    `scripts/capture/ravel-lite-tutorial.sh` must be proven runnable on a
    live TestAnyware VM (`VNC_ENDPOINT`/`AGENT_ENDPOINT` validated, eight
    named `[STEP-NAME]` sections completing without error). See memory entry
    `capture-scripts-template-documentation-with-real-system-runs` for the
    current known-approximation status of the script.

    ## Scope

    - Run `scripts/capture/ravel-lite-tutorial.sh` on a TestAnyware VM.
    - Extract per-section terminal output from the session log.
    - Replace `----` illustrative listing blocks in chapters 01–05 with
      `[source,console]` blocks containing the real output.
    - Re-render with `scripts/render-docs.sh` and verify HTML output.

    ## Done when

    All five tutorial chapters use verbatim captured output and the rendered
    HTML passes a visual spot-check.
  results: |
    ## Scope executed (option B)

    User unblocked the task ("TestAnyware has a vm, and that is the only issue unblocked") and chose option B from a three-way scoping menu: extend the capture script for terminal-text capture and validate it against the real `testanyware` CLI surface, without spending live VM time. The task's full scope (live run + per-chapter block replacement) was deliberately deferred to a follow-up phase.

    ## What changed

    **`scripts/capture/ravel-lite-tutorial.sh`** — substantive rewrite:

    - **Endpoint discovery removed.** Probing `testanyware vm list --help` showed the subcommand accepts NO flags — `--format json` does not exist. The previous `vm list --format json | jq` pipeline would have failed at first contact with a live VM. Replaced with deterministic `VM_ID="ravel-lite-tutorial-$(date -u +%Y%m%dT%H%M%SZ)"` passed to `vm start --id` and reused via `--vm "$VM_ID"` thereafter; testanyware itself resolves `--vm <id>` against its own per-VM spec at `$XDG_STATE_HOME/testanyware/vms/<id>.json`. The `VNC_ENDPOINT`/`AGENT_ENDPOINT` env-var override block and the `jq` dependency are gone.
    - **Transcript capture added.** New `transcripts/` output dir under `docs/captures/ravel-lite-tutorial/`, plus a `transcript_at <label> <command>` helper that runs the command on the VM with `RAVEL_LITE_CONFIG` pre-set and captures combined stdout+stderr to `<label>.txt`. The transcript leads with `$ <command>` so files drop paste-ready into `[source,console]` blocks; the env-var bookkeeping is added at the wire level only, kept off the displayed line so transcripts match the chapters' user-perspective phrasing.
    - **Chapter coverage**: chapters 01 (`install-and-config`) and 02 (`the-project`) now have explicit transcript captures wired (`01-version`, `01-init-fresh`, `01-init-refuse`, `01-init-force`, `01-ls-phases`, `01-projects-list-empty`, `02-git-init`, `02-git-commit`, `02-projects-add`, `02-projects-list-populated`). Tolerates non-zero exit (the `01-init-refuse` case is expected to fail).
    - **TUI flow unchanged in shape, retargeted to `--vm`.** Chapters 04–05 stay screenshot-driven because TUI stdout lives inside the VM's GUI Terminal, not on the agent channel. Chapter 03 (`ravel-lite create`) is not yet captured — also interactive, deferred to next iteration.
    - **Header doc rewritten** to explain endpoint plumbing, the headless-vs-TUI capture split, and what's deliberately not in scope yet.

    ## Validation done

    - Probed every `testanyware` 0.2.0 subcommand the script invokes (`vm start`, `vm stop`, `exec`, `screenshot`, `find-text`, `download`, `input type`, `input key`) and confirmed every flag in the rewritten script matches.
    - `bash -n` syntax check passes.
    - No `shellcheck` available locally, so no lint-level pass beyond `bash -n`.

    ## What did NOT happen (and why)

    - **No live VM run.** Option B was specifically scoped to avoid the wall-clock and VM-budget cost of a real run. The script is now ready for one but hasn't been exercised end-to-end.
    - **No chapter edits.** The 58 `----` listing delimiters across `docs/tutorial/01..05*.adoc` are still illustrative content; replacing them with verbatim captures is the next-phase deliverable.
    - **No memory update.** The existing memory entry `capture-scripts-template-documentation-with-real-system-runs` still describes the old approximation; updating it is reflect's job, not work's.

    ## What this suggests next

    - A dedicated phase to run the script live, debug whatever real-VM behaviour diverges from `--help`-deduced assumptions, and fold the resulting `transcripts/` files into chapters 01–02 as a first cut.
    - Chapter 03 (`ravel-lite create`) needs a capture mechanism that handles interactive prompt-and-response — likely `testanyware input type` on a real terminal with `find-text` waits, plus `script(1)` recording inside the VM if we want full text fidelity. Worth scoping separately from the headless 01-02 work.
    - Memory entry `capture-scripts-template-documentation-with-real-system-runs` is now factually wrong on the "vm list --format json approximation" — reflect should rewrite it to reflect the post-rewrite design (deterministic `--id` + `--vm` resolution, no JSON parsing) so the historical "approximation" framing doesn't mislead future drift checks.
