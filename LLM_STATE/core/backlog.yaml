tasks:
- id: diagnose-claude-tui-invisible-after-work-phase-banner-on-a-fresh-machine
  title: 'Verify fix: re-test work-phase TUI hang on fresh machine after removing --add-dir and fixing path normalisation'
  category: infra
  status: blocked
  blocked_reason: No fresh-machine access right now; revisit when colleague is available to re-test
  dependencies: []
  description: |
    ## Status

    Both leading-hypothesis fixes have shipped:

    1. **Double-slash path-join bug** (`select_config_dir` now normalises trailing slashes via `components().collect()`).
    2. **Redundant `--add-dir <plan_dir>`** removed from `invoke_interactive` in `src/agent/claude_code.rs`.

    These were the two most likely causes of the invisible trust-grant modal (hypothesis A) that hung the work phase on a fresh machine.

    ## Required action

    Have the affected colleague (macOS, local terminal, no multiplexer, Claude 2.1.119) pull the latest binary and re-run `ravel-lite run` from their launcher-web project.

    Expected outcome: work phase proceeds normally with no hang after the banner.

    ## If the hang persists

    Pivot to hypothesis B (inline-viewport handoff bug in `src/ui.rs:298` reset volley). Investigation path:

    - Check colleague's `~/.claude.json` for the project path to see if trust is already granted.
    - Run `dangerous: true` in their plan's config (appends `--dangerously-skip-permissions`); if hang disappears, a remaining trust-grant modal is the cause.
    - If B, inspect `src/ui.rs:244` Viewport::Inline and the `suspend` volley at line 298.

    The `capture-full-claude-stdio-under-a-pty-in-debug-log-for-tui-hang-diagnosis` task provides the full diagnostic capture tool if further investigation is needed.

    ## What "done" looks like

    Colleague confirms the hang is gone. Or, if hang persists: root cause identified and a follow-up fix landed.
- id: add-pty-capture-debug-path-to-pi-rs-invoke-interactive-parity-with-claude-code-rs
  title: Add PTY-capture debug path to pi.rs::invoke_interactive (parity with claude_code.rs)
  category: infra
  status: blocked
  blocked_reason: Not needed yet; defer until pi-side TUI debugging actually requires it
  dependencies: []
  description: |
    Add the same PTY-capture path to `pi.rs::invoke_interactive` that was
    built for `claude_code.rs` in the PTY-capture task. The mechanism is
    already implemented in `src/agent/pty_capture.rs`; this task is a
    small scope wiring job.

    ## Why

    When debug logging is enabled, `claude_code.rs::invoke_interactive` now
    routes through `pty_capture::run_pty_session`, capturing every byte
    exchanged with the claude subprocess in the debug log. The `pi.rs`
    equivalent still uses inherited stdio (`stdio: inherited (no transcript
    available)` per the Results from the PTY-capture task). A pi TUI hang
    would have the same diagnostic blind spot the claude hang had before the
    PTY work shipped.

    ## Scope

    - In `src/agent/pi.rs::invoke_interactive`, add an early branch:
      when `debug_log::is_enabled()`, call
      `pty_capture::run_pty_session("pi", &args, &cwd, "pi")` via
      `spawn_blocking`, matching the pattern in `claude_code.rs`.
    - Existing argv construction is shared; only the spawn mechanism differs.
    - Update the log entry text to read `stdio: pty (full byte transcript
      follows)` on the debug path, matching the claude path.

    ## Verification

    - `cargo test` passes (all PTY-capture unit/integration tests still
      pass; the pi tests are unaffected because none exercise the debug path).
    - Non-debug pi invocation: TUI renders identically, no behaviour change.
    - Debug-enabled pi invocation: TUI renders identically AND debug log
      captures the pi byte stream.
- id: replace-tutorial-illustrative-transcripts-with-testanyware-captured-transcripts
  title: Run capture script live and replace chapters 01-02 tutorial transcripts with TestAnyware output
  category: docs
  status: blocked
  blocked_reason: 'Capture script extended and validated against testanyware 0.2.0 CLI surface; no live VM run yet. Remaining: run the script against a live TestAnyware VM and fold transcripts into chapters 01-02.'
  dependencies: []
  description: |
    Run `scripts/capture/ravel-lite-tutorial.sh` against a live TestAnyware VM to produce
    verbatim terminal transcripts, then fold those transcripts into tutorial chapters 01 and 02.

    ## What the script does

    `scripts/capture/ravel-lite-tutorial.sh` drives a TestAnyware macOS VM: Homebrew-installs
    ravel-lite, runs each chapter-01 and chapter-02 command via `transcript_at`, and writes
    combined stdout+stderr to `docs/captures/ravel-lite-tutorial/transcripts/<label>.txt` in
    paste-ready `[source,console]` AsciiDoc format.

    Eleven named captures cover chapters 01-02: `01-version`, `01-init-fresh`, `01-init-refuse`,
    `01-init-force`, `01-ls-phases`, `01-projects-list-empty`, `02-git-init`, `02-git-commit`,
    `02-projects-add`, `02-projects-list-populated`.

    ## Scope

    - Run the script against a live VM; debug any divergences from `--help`-deduced assumptions.
    - Verify all 11 captures land in `docs/captures/ravel-lite-tutorial/transcripts/`.
    - Replace each `----` illustrative listing block in `docs/tutorial/01-install-and-config.adoc`
      and `docs/tutorial/02-the-project.adoc` with `[source,console]` blocks containing the
      corresponding transcript file's content.
    - Re-render with `scripts/render-docs.sh` and spot-check HTML output.

    ## Out of scope

    - Chapter 03 (`ravel-lite create`) is interactive; a separate interactive capture mechanism
      is required before chapter 03 transcripts can be produced.
    - Chapters 04-05 are TUI-screenshot-driven; those updates are a separate task.

    ## Done when

    Chapters 01 and 02 use verbatim captured output and the rendered HTML passes a visual spot-check.
  results: |
    ## Scope executed (option B)

    User unblocked the task ("TestAnyware has a vm, and that is the only issue unblocked") and chose option B from a three-way scoping menu: extend the capture script for terminal-text capture and validate it against the real `testanyware` CLI surface, without spending live VM time. The task's full scope (live run + per-chapter block replacement) was deliberately deferred to a follow-up phase.

    ## What changed

    **`scripts/capture/ravel-lite-tutorial.sh`** — substantive rewrite:

    - **Endpoint discovery removed.** Probing `testanyware vm list --help` showed the subcommand accepts NO flags — `--format json` does not exist. The previous `vm list --format json | jq` pipeline would have failed at first contact with a live VM. Replaced with deterministic `VM_ID="ravel-lite-tutorial-$(date -u +%Y%m%dT%H%M%SZ)"` passed to `vm start --id` and reused via `--vm "$VM_ID"` thereafter; testanyware itself resolves `--vm <id>` against its own per-VM spec at `$XDG_STATE_HOME/testanyware/vms/<id>.json`. The `VNC_ENDPOINT`/`AGENT_ENDPOINT` env-var override block and the `jq` dependency are gone.
    - **Transcript capture added.** New `transcripts/` output dir under `docs/captures/ravel-lite-tutorial/`, plus a `transcript_at <label> <command>` helper that runs the command on the VM with `RAVEL_LITE_CONFIG` pre-set and captures combined stdout+stderr to `<label>.txt`. The transcript leads with `$ <command>` so files drop paste-ready into `[source,console]` blocks; the env-var bookkeeping is added at the wire level only, kept off the displayed line so transcripts match the chapters' user-perspective phrasing.
    - **Chapter coverage**: chapters 01 (`install-and-config`) and 02 (`the-project`) now have explicit transcript captures wired (`01-version`, `01-init-fresh`, `01-init-refuse`, `01-init-force`, `01-ls-phases`, `01-projects-list-empty`, `02-git-init`, `02-git-commit`, `02-projects-add`, `02-projects-list-populated`). Tolerates non-zero exit (the `01-init-refuse` case is expected to fail).
    - **TUI flow unchanged in shape, retargeted to `--vm`.** Chapters 04–05 stay screenshot-driven because TUI stdout lives inside the VM's GUI Terminal, not on the agent channel. Chapter 03 (`ravel-lite create`) is not yet captured — also interactive, deferred to next iteration.
    - **Header doc rewritten** to explain endpoint plumbing, the headless-vs-TUI capture split, and what's deliberately not in scope yet.

    ## Validation done

    - Probed every `testanyware` 0.2.0 subcommand the script invokes (`vm start`, `vm stop`, `exec`, `screenshot`, `find-text`, `download`, `input type`, `input key`) and confirmed every flag in the rewritten script matches.
    - `bash -n` syntax check passes.
    - No `shellcheck` available locally, so no lint-level pass beyond `bash -n`.

    ## What did NOT happen (and why)

    - **No live VM run.** Option B was specifically scoped to avoid the wall-clock and VM-budget cost of a real run. The script is now ready for one but hasn't been exercised end-to-end.
    - **No chapter edits.** The 58 `----` listing delimiters across `docs/tutorial/01..05*.adoc` are still illustrative content; replacing them with verbatim captures is the next-phase deliverable.
    - **No memory update.** The existing memory entry `capture-scripts-template-documentation-with-real-system-runs` still describes the old approximation; updating it is reflect's job, not work's.

    ## What this suggests next

    - A dedicated phase to run the script live, debug whatever real-VM behaviour diverges from `--help`-deduced assumptions, and fold the resulting `transcripts/` files into chapters 01–02 as a first cut.
    - Chapter 03 (`ravel-lite create`) needs a capture mechanism that handles interactive prompt-and-response — likely `testanyware input type` on a real terminal with `find-text` waits, plus `script(1)` recording inside the VM if we want full text fidelity. Worth scoping separately from the headless 01-02 work.
    - Memory entry `capture-scripts-template-documentation-with-real-system-runs` is now factually wrong on the "vm list --format json approximation" — reflect should rewrite it to reflect the post-rewrite design (deterministic `--id` + `--vm` resolution, no JSON parsing) so the historical "approximation" framing doesn't mislead future drift checks.
- id: design-and-implement-interactive-capture-mechanism-for-tutorial-chapter-03-ravel-lite-create
  title: Design and implement interactive capture mechanism for tutorial chapter 03 (ravel-lite create)
  category: docs
  status: blocked
  blocked_reason: Mechanism implemented; live VM run + chapter 03 block replacement deferred to the same VM-time-needed phase as the chapter 01-02 transcripts task
  dependencies: []
  description: |
    Design and implement a capture mechanism for `docs/tutorial/03-creating-a-plan.adoc`
    (`ravel-lite create`), which drives an interactive prompt-and-response TUI session.
    Unlike chapters 01-02, `ravel-lite create` is not headless; stdout is inside the VM's
    GUI Terminal, not on the TestAnyware agent channel.

    ## Why a separate task

    `transcript_at` (non-interactive command capture) cannot capture the create-session
    exchange. Chapter 03 illustrative transcripts show multi-turn dialogue; byte-accurate
    capture requires a different mechanism.

    ## Design options to evaluate

    - `testanyware input type` to send keystrokes + `testanyware find-text` waits to detect
      prompts, combined with `script(1)` recording inside the VM for full byte fidelity.
    - Screenshot-based approach matching chapters 04-05.
    - PTY recording piped out via `testanyware exec` if the VM runner supports it.

    ## Done when

    A mechanism is implemented in `scripts/capture/ravel-lite-tutorial.sh`, validated
    against a live VM, and chapter 03's `----` illustrative blocks are replaced with
    verbatim captures or screenshots as appropriate.
  results: |
    ## Scope executed

    Designed and implemented an interactive-capture mechanism for chapter 03
    (`ravel-lite create`) inside `scripts/capture/ravel-lite-tutorial.sh`,
    matching the predecessor task's option-B precedent: build the
    mechanism, defer the live VM run and chapter rewrite to a follow-up
    that consumes a real VM.

    ## Design choice (hybrid)

    Chapter 03's content splits cleanly into two regimes; the mechanism
    matches each regime to the right capture form:

    - **Interactive scope conversation** — claude paraphrases each of the
      four scope questions (`defaults/create-plan.md` §1 gives claude a
      bullet list of topics, not literal prompt strings), so byte-accurate
      text capture would rot every time claude's wording shifted. Captured
      via screenshots on each prompt moment, with scripted responses
      driven from pre-staged response files. The chapter's existing
      "illustrative slice" framing remains correct.
    - **Post-create on-disk state** — `ls`, `state backlog list`,
      `state memory list` outputs are deterministic given the responses
      we feed claude. Captured via the existing `transcript_at` mechanism.

    Other options considered and rejected:

    - `script(1)` inside the VM with ANSI-stripping post-processing —
      rejected because the claude-code interactive prompt is a TUI with
      heavy ANSI redraws; even with `col -bp` plus sed for SGR sequences
      the output would be noisy and brittle.
    - Pure screenshots for the entire chapter — rejected because the
      post-create state IS deterministic and text-renderable; downgrading
      to images for that section would harm tutorial readability for no
      benefit.
    - `expect(1)` inside the VM — rejected because it would add an
      in-VM dependency for marginal gain over the input/find-text driver
      we already have.

    ## What changed

    **`scripts/capture/ravel-lite-tutorial.sh`** (196 → 278 lines):

    - New `RESPONSES_DIR` constant pointing at `scripts/capture/responses/`.
    - New `type_lines <response-file>` helper — types each line of a file
      via `testanyware input type` with `input key return` between lines.
      Avoids relying on `input type` carrying through embedded newlines
      (the CLI takes a single `<text>` argument; line-then-return is the
      predictable contract claude's input box sees).
    - New `drive_scope_question <label> <wait-text>` helper — `find-text`
      wait, screenshot, then `type_lines` from `responses/03-<label>.txt`.
      Wait substrings are the most stable topic word per question and may
      need tuning on first live run.
    - New `capture_chapter03_create_session` function — opens Terminal,
      invokes `ravel-lite create LLM_STATE/main`, drives the four scope
      questions, then waits for an injected `echo CHAPTER_03_CREATE_DONE`
      marker (deterministic post-claude-exit signal) before screenshotting
      completion. The marker is the sync point that guarantees the
      post-create files exist on disk before the next step queries them.
    - New `capture_chapter03_post_state` function — `transcript_at` on
      `ls`, `state backlog list`, `state memory list` against the freshly
      created plan.
    - `scenario_run` cleaned up: previously invoked `ravel-lite create`
      with no argument and waited for a non-existent "plan name" prompt
      (latent bug — same shape as the previous task's `vm list --format
      json` assumption that would fail at first contact with a live VM).
      Now only drives `ravel-lite run` against the plan that chapter 03
      already created.
    - `main()` wired to call `capture_chapter03_create_session` then
      `capture_chapter03_post_state` between chapter 02 and chapter 04.
    - Header doc updated with a "Chapter 03" subsection explaining the
      hybrid design and the marker-echo completion signal; outputs comment
      updated to note `screens/` now covers chapter 03 too.

    **`scripts/capture/responses/03-{purpose,project,backlog,memory-seed}.txt`**
    — four new files. Verbatim copies of the illustrative responses from
    `docs/tutorial/03-creating-a-plan.adoc` lines 32-34, 38-41, 45-50,
    54-62 respectively. Authored content (NOT generated capture output);
    they live under `scripts/capture/` rather than `docs/captures/` so
    they fall outside `.gitignore`'s `docs/captures/` rule and get tracked
    in git.

    ## Validation done

    - `bash -n` syntax check passes.
    - `shellcheck` not available locally (same constraint as the previous
      task); no lint-level pass beyond `bash -n`.
    - Verified `ravel-lite create --help` against my invocation
      (`ravel-lite create LLM_STATE/main` with `cd` into the project
      directory) — single positional `<PLAN_DIR>` argument, parent must
      exist, target must not. The chapter 02 setup creates the parent.
    - `.gitignore` already covers `docs/captures/`, so the new chapter 03
      screens / transcripts will land there ignored. `scripts/capture/responses/`
      is correctly NOT ignored (authored content).

    ## What did NOT happen (and why)

    - **No live VM run.** Same blocker as the predecessor task — VM time
      is the constraint, not mechanism readiness.
    - **No chapter 03 edits.** The `[source,text]` conversation block and
      the `[source,bash]` post-create blocks remain illustrative. Editing
      them requires a successful live run to produce real captures (the
      `ls`/backlog/memory transcripts will differ in detail from the
      imagined ones — claude infers task titles, categories, and memory
      entry IDs from our responses but won't reproduce the chapter's text
      byte-for-byte).
    - **No memory updates.** The reflect phase owns memory writes; this
      phase only implements.

    ## What this suggests next

    - A live-VM phase that runs the script end-to-end against a real
      TestAnyware VM, debugs whichever `find-text` wait substrings fail
      (claude's paraphrasing is the highest-risk axis), and folds the
      resulting captures into chapters 01-02 AND 03 in one pass. The
      predecessor task and this task are now coupled around the same
      pending live run; triage should consider merging them or marking
      this task as a dependency of the predecessor.
    - If on first live run the four scope-question wait substrings prove
      unstable (claude saying "tell me about your codebase" instead of a
      prompt containing "project", say), the fix is to either (a) widen
      the substring with a regex, (b) tighten the create-plan template
      itself to make claude's wording more deterministic, or (c) add
      intermediate "what is claude waiting for" probes via screenshots +
      manual labelling. (b) is probably right — the template already
      paraphrases freely; pinning the prompt wording would help capture
      AND give users a more consistent first-time experience.
    - The chapter-03 conversation block may want to switch from
      `[source,text]` to a screenshot-driven block once we see what
      claude actually produces. Decide that after the live run.
    - The injected marker (`echo CHAPTER_03_CREATE_DONE`) is a pattern
      worth generalising — any future capture that drives an interactive
      command in the GUI Terminal will have the same "when did it finish"
      problem, and a marker-echo + find-text idiom is the cleanest signal
      available without instrumenting the orchestrator.
