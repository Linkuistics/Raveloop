### Session 2 (2026-04-17T14:28:05Z) — resolve 4 backlog tasks: work model, survey timeout, skills move, phase contract test

- Attempted four tasks: explicit work-phase model default, survey invocation timeout, orphaned skills files, phase→file-write integration test
- All four completed successfully; full test suite passes
- `defaults/agents/claude-code/config.yaml`: set `work: claude-opus-4-7` (was empty string, silently delegating to whatever claude picks at spawn); extended `embedded_defaults_are_valid` test to assert every (agent, phase) pair has a non-empty model string
- `src/survey/invoke.rs`: wrapped stdout read in `tokio::time::timeout` with `DEFAULT_SURVEY_TIMEOUT_SECS = 300`; added `resolve_timeout(Option<u64>) -> Duration` helper with two unit tests; on timeout the child is killed and the error names elapsed time, captured bytes, partial stdout, and remediations; added `--timeout-secs` CLI flag threaded through `src/main.rs`
- `defaults/skills/{brainstorming,tdd,writing-plans}.md` → `defaults/agents/pi/subagents/`: discovered these files are pi subagent definitions, not claude-code skills; moved them to the correct location, updated `src/init.rs` embed paths, updated `src/agent/pi.rs` to read from new location, updated `docs/architecture.md` to reflect the tree change
- `tests/integration.rs`: added `ContractMockAgent` + `phase_contract_round_trip_writes_expected_files`; test installs real embedded defaults, seeds a plan at `phase=analyse-work`, runs `phase_loop` with a mock that writes the files each phase prompt requires; 6 assertions cover latest-session.md, commit-message.md consumed, memory.md updated by reflect, backlog.md updated by triage, phase.md ends at `work`, git log has expected commit subjects
- New backlog task added: "Reliably mark completed backlog items as `done`" — work-phase prompt never instructs the agent to flip the Status: line, causing silent staleness
- Key learning: the skills/ directory name was misleading — pi was already reading it as subagent definitions; moving to `agents/pi/subagents/` makes the ownership and structure explicit
- Next: pi agent scope decision (hard blocker on pi use due to `{{MEMORY_DIR}}` unresolved token); reliably marking tasks done (prompt + analyse-work safety net)
